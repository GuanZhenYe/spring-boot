spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/GuanZhenYe/springcloud-config/
          search-paths: config
          username:
          password:
      label: master

server:
  port: 8768
